<%

function DrawMessages()
{
  for (var k in __this.items)
  {
    var v = __this.items[k];
    v.account_id = __this.account_id;

    this.CascadeDesign('thread/chatbox/one', v);
  }
}

__this.CascadeDesign('snippets/reverse', {cb: DrawMessages});
%>
