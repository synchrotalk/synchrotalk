<%
__this.CascadeDesign('utils/image/mini', __this);
__this.DeferCascade(function()
{
  this.first().click(function()
  {
    $('body').append(phoxy.DeferRender('utils/modal/image', __this));
  });
});
%>
