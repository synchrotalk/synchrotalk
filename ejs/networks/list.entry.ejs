<div class='list entry networks <%@ __this.name %>'>
  <span class='image'>
    <% __this.CascadeDesign('networks/icon.by.name', __this) %>
  </span>
  <span class='name'>
    <%@ __this.name %>
  </span>
</div>

<%
__this.Defer(function()
{
  __this.first().on('click select', function(e)
  {
    __this.first().trigger('network.choose', __this.name);
  });
});
%>

