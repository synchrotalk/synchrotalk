<%
__this.CascadeDesign('utils/dimmer/dimmer.attention',
  {
    content: "Hello " +  __this.user.nickname,
    next: __this.next,
  });

__this.DeferCascade(function()
{
  function hide()
  {
    phoxy.Defer(function()
    {
      __this.first().dimmer('hide');
    }, 3000);
  }

  __this.first()
    .dimmer
      ({
        onShow: hide
      })

  if (!__this.first().dimmer('is animating'))
    hide();
});
%>
